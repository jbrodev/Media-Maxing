"""AI provider abstraction for the local-social-ai-manager.

Batch 3 keeps real provider calls disabled by default. Mock is the only
provider that runs end-to-end. Real provider adapters are scaffolded but
remain gated behind explicit environment configuration.
"""
