# Packages

Shared workspace packages live here.

- `shared`: Shared utilities and domain logic.
- `ui`: Shared UI components.
- `types`: Shared TypeScript or schema types.
- `config`: Shared configuration.
